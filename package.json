{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./backend/api-server\" --filter \"./frontend/pottery-store\" --filter \"./scripts\" --if-present run typecheck",
    "dev": "pnpm -r --filter \"./backend/api-server\" --filter \"./frontend/pottery-store\" dev"
  },
  "private": true,
  "devDependencies": {
    "prettier": "^3.8.3",
    "typescript": "~5.9.3"
  },
  "dependencies": {
    "@types/bcryptjs": "^3.0.0",
    "@types/node": "catalog:",
    "bcryptjs": "^3.0.3",
    "mongoose": "catalog:",
    "ts-node": "^10.9.2"
  }
}
